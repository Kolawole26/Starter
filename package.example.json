{
  "scripts": {
    "build": "infisical run --env=dev --path=/Base -- nuxt dev",
    "dev": "infisical run --env=dev --path=/Base -- nuxt dev",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  }
}
